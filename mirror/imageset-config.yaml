apiVersion: mirror.openshift.io/v1alpha2
kind: ImageSetConfiguration
archiveSize: 4
mirror:
 operators:
   - catalog: registry.redhat.io/redhat/redhat-operator-index:v4.10
     packages:
     - name: openshift-gitops-operator
       channels:
       - name: latest
storageConfig:
 local:
   path:  /home/dshtranv/operators
